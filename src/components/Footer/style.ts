import { styled } from "@material-ui/system";

export const Container = styled("footer")({
  gridArea: "footer",
  display: "flex",
  height: "80px",
  justifyContent: "center",
  alignItems: "center",
  gap: "10px"
});
