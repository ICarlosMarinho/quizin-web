import { styled } from "@mui/material";

export const Container = styled("footer")({
  gridArea: "footer",
  display: "flex",
  height: "80px",
  justifyContent: "center",
  alignItems: "center",
  gap: "10px"
});
